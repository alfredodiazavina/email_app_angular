import { MessageButtonDirective } from '../../message-button.directive';

describe('MessageButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageButtonDirective();
    expect(directive).toBeTruthy();
  });
});
