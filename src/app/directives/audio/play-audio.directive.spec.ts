import { PlayAudioDirective } from '../../play-audio.directive';

describe('PlayAudioDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayAudioDirective();
    expect(directive).toBeTruthy();
  });
});
